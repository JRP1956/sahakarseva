# Admin — Federation dashboard (Next.js)

```bash
cd admin
npm install
npm run dev          # http://localhost:3000 — needs the backend on :8000
```

Login: `9999999999` / `pass123`. Set `API_URL` in `.env.local` if the backend isn't on localhost:8000.
